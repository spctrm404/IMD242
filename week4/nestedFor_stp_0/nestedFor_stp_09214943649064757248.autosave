float spacing;

void setup() {
  size(640, 480);
  noStroke();
  fill(255);
  spacing = 20;
}

void draw() {
  background(0);

  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 0 + spacing * 0.5, spacing);
  }
}
