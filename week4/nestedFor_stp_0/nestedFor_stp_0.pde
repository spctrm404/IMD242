float spacing;
float rad;

void setup() {
  size(640, 480);
  fill(255);
  noStroke();
  spacing = 20;
  rad = 8;
}

void mousePressed() {
  loop();
}

void draw() {
  background(0);

  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 0 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 1 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 2 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 3 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 4 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 5 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 6 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 7 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 8 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 9 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 10 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 11 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 12 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 13 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 14 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 15 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 16 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 17 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 18 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 19 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 20 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 21 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 22 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 23 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 24 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 25 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 26 + spacing * 0.5, 2 * rad);
  }
  for (int x = 0; x < width; x += spacing) {
    circle(x + spacing * 0.5, spacing * 27 + spacing * 0.5, 2 * rad);
  }
}
